<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% for(let url in urls){ %>
        <% if(url === shortURL){ %>
            <h3><a href='<%= `${urls[url]}`%>'><%=`${urls[url]}`%></a><%=` can now be accessed at `%><a href="http://localhost:8080/u/<%=`${url}`%>"><%=`http://localhost:8080/u/${url}`%></a></h3>
        <% } %>
    <% } %>
</body>
</html>