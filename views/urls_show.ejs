<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% for(let url in urls){ %>
        <% if(url === shortURL){ %>
            <%= `${urls[url]} ==> ${url}`%>
        <% } %>
    <% } %>
</body>
</html>